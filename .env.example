# Copy this file to .env and fill in your values
# cp .env.example .env

# ── Required ──────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# ── Optional (server mode only) ───────────────────
PORT=3000

# ── Optional (CLI mode only) ──────────────────────
# INPUT_DIR=./input      # folder of PDF articles
# OUTPUT_DIR=./output    # where docx files are saved
